[package]
name = "bs-viz-sdk"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Crate-author SDK for BugStalker visualisers (Tier A — declarative specs)."

[dependencies]
bs-viz-spec = { path = "../bs-viz-spec" }
bs-viz-derive = { path = "../bs-viz-derive" }
